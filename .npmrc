# @sanity/color-input@4 declares peer `sanity@^3 || ^4`, but this project is on
# sanity@5, so a default `npm install`/`npm ci` fails on peer resolution.
# This pins the flag the project already relies on (Vercel sets it out-of-band)
# so installs are reproducible locally and in CI.
legacy-peer-deps=true
