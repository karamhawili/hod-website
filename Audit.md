# Audit Tracker — HOD Redesign (Phase 1.5)

Living record of audit findings and their remediation status. Companion to the Phase 1 audit report.

**Legend:** ⬜ TODO · 🟦 IN PROGRESS · ✅ DONE · ⛔ BLOCKED (needs decision) · ⏳ DEFERRED (belongs to a later phase) · ❌ WON'T-FIX

---

## 🔴 Critical

> Reality-check: none of the four criticals is a present-tense bug. C1–C3 are **constraints for the Phase 3 Nav/Footer rebuild** — there is no safe code change to land today without starting that rebuild (which also skips Phase 2 token work). C4 is the only Phase-1.5-actionable item, and it is gated on the typegen go/no-go decision.

| #   | Status                | Item                                                                          | Notes / plan                                                                                                                                                                                                                                       |
| --- | --------------------- | ----------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | ✅ DONE (Phase 6)     | Footer `showGradient` consumed by out-of-scope `/project/[slug]` & `/join-us` | Prop **removed** in Phase 6: the rebuilt `/project/[slug]` renders no Footer at all; the last call site (`/portfolio`) updated to plain `<Footer />` (1-line flagged touch).                                                                       |
| C2  | ✅ DONE (Phase 6)     | Navigation `theme` consumed by out-of-scope `/project/[slug]`                 | Prop **removed** in Phase 6: the rebuilt detail page renders no Navigation (VVD: close ✕ only). The rail's scroll listener existed solely for the light-flip and went with it.                                                                     |
| C3  | ✅ RESOLVED BY DESIGN | Nav/Footer rendered per-route (5 files), not in layout                        | Closed in Phase 6: the detail page intentionally renders **no chrome at all**, so a shared-layout hoist is permanently wrong-shaped. Per-route rendering is the intended architecture, not tech debt.                                              |
| C4  | ✅ DONE               | `@/types/sanity` imported by 10 out-of-scope files → type change ripples      | **Adopted TypeGen without rippling into out-of-scope files.** Pipeline configured, `src/sanity/sanity.types.ts` generated & committed, `tsc --noEmit` clean. Consumer migration deferred by design (see below).                                    |

**C4 — ADOPTED TYPEGEN. What shipped:**

- ✅ `typegen` block in `sanity.cli.ts` (`path`, `schema` → `src/sanity/extract.json`, `generates` → `src/sanity/sanity.types.ts`). This is the actual **S5** fix — the script was fine; the missing config told `typegen generate` where to read the schema.
- ✅ Gitignored the intermediate `src/sanity/extract.json`; committed the generated `src/sanity/sanity.types.ts` (911 lines).
- ✅ **`overloadClientMethods: false`** — critical setting. `true` retypes every existing `sanityFetch()` to generated types, which conflicts with the hand-written `PageBuilderBlock[]` still consumed by the out-of-scope project page and **breaks `next build`** (verified: 1 error at `project/[slug]/page.tsx`). With `false`, existing code compiles unchanged and generated `*_QUERY_RESULT` types are available for opt-in import.
- ✅ Verified `npx tsc --noEmit` is clean (0 errors) after the change. (A stale `tsconfig.tsbuildinfo` briefly masked this — cleared it.)
- ⏳ **Migrate consumers** — deferred by design. In-scope files adopt generated types during their rebuild (Phase 3/4); out-of-scope PageBuilder/project files keep importing `@/types/sanity` until those pages are migrated (the exact ripple C4 warns about). Flip `overloadClientMethods` back to `true` once every consumer uses generated types.

> Note: the committed `sanity.types.ts` matches `overloadClientMethods: false`, so re-running `npm run typegen` reproduces it identically.

---

## 🟠 Should-fix (awaiting green light after critical review)

| #   | Status                            | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| S1  | ✅ DONE                           | Split `queries.ts` into `LIST_FIELDS` (no page-builder `content`) for the three list queries + `DETAIL_FIELDS` (listing + content) for `PROJECT_PAGE_QUERY`. Cards no longer fetch the full project-page payload. Verified: no `content` consumer among list callers, `tsc` clean, detail query unchanged in shape. **Follow-up:** re-run `npm run typegen` to refresh the generated `*_QUERY_RESULT` shapes (harmless while stale — nothing consumes them with `overloadClientMethods: false`). |
| S2  | ⏳ DEFERRED → Phase 4 (confirmed) | Waterfall in home `page.tsx`. `page.tsx` is wiped in Phase 4 → build with `Promise.all`/parallel fetch from the start. See carry-forward.                                                                                                                                                                                                                                                                                                                                                        |
| S3  | ⏳ DEFERRED → Phase 4 (confirmed) | Unnecessary `"use client"` on wiped components → new landing components default to server components. See carry-forward.                                                                                                                                                                                                                                                                                                                                                                         |
| S4  | ⏳ DEFERRED → Phase 4 (confirmed) | Image opt. In-scope targets wiped → build optimized from the start. Out-of-scope `LatestProjects`/`ProjectsGrid` left untouched (flag only). See carry-forward.                                                                                                                                                                                                                                                                                                                                  |
| S5  | ✅ DONE                           | ~~`typegen` npm script is misconfigured~~ Fixed by adding the `typegen` block to `sanity.cli.ts` (done as part of C4). Config loads; full run pending creds (see C4).                                                                                                                                                                                                                                                                                                                            |
| S6  | ✅ DONE                           | **New (surfaced in Phase 1.5):** `npm install`/`npm ci` fail without `--legacy-peer-deps`. Mitigated with `.npmrc` (`legacy-peer-deps=true`). **Update (Phase 4):** `@sanity/color-input` removed entirely (its consumer, `category.color`, was dropped) — but the `.npmrc` flag must STAY: attempting removal exposed deeper ecosystem conflicts (`@sanity/vision`→`@portabletext/editor` wants `react@^19.2.3` vs pinned `19.2.0`; `next-sanity@11.6` vs `sanity@5.11`). Full fix = coordinated Sanity-ecosystem + React upgrade — deferred to Phase 9 / separate chore.                 |

---

## 🟡 Nice-to-have (backlog)

| #   | Status  | Item                                                                                                                                  |
| --- | ------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| N1  | ⬜ TODO | `coalesce(location, category)` references a non-existent `category` field — dead fallback.                                            |
| N2  | ⬜ TODO | `urlFor(...).url() \|\| ""` empty-string `src` → broken `<Image>` if image missing.                                                   |
| N3  | ⬜ TODO | GROQ orders by `_createdAt` but components re-sort by `year`; missing `sizes` on scaled images.                                       |
| N4  | ⬜ TODO | Live API has no tokens / no `<VisualEditing/>` → no in-Studio preview. Fine for published-only; revisit if editors want live preview. |
| N5  | ⬜ TODO | Footer LinkedIn points to `company/addmindhospitality` — verify correct company.                                                      |
| N6  | ⬜ TODO | `useScrollAnimation` makes `Section` a client component site-wide; re-evaluate for the rebuild.                                       |
| N7  | ⬜ TODO | **Manual ordering for projects** (user-requested backlog, 2026-07-25): landing rotation + archive currently order by `_createdAt desc` — newest always leads. Adopt manual ordering (e.g. `@sanity/orderable-document-list` / orderRank) so the client controls what leads.              |

---

## Phase 4 carry-forward (build it right, don't fix-then-delete)

The new landing/about components must be built to satisfy these — they replace the deferred should-fixes:

- **[S2]** Fetch project data in the server page with `Promise.all` (no sequential `await` waterfall). With `LIST_FIELDS` (S1 ✅) the payload is already trimmed.
- **[S3]** New components are **server components by default**; add `"use client"` only where there's real interactivity (state, effects, handlers).
- **[S4]** Standardize a Sanity image helper (`.width(...).auto('format')`, sane `quality`, proper `sizes`) and use it everywhere instead of raw `urlFor().url()` + `quality={100}`.
- **Also revisit (nice-to-have):** N2 (`url() || ""` empty-src), N3 (`sizes`, redundant client-side re-sort), N6 (`Section` client-only animation hook — CLAUDE.md flags re-evaluating `useScrollAnimation`).

**Also carried by out-of-scope coupling (from Criticals):** rebuilt Navigation must keep the `theme` prop (C2); rebuilt Footer must keep accepting `showGradient` (C1) until `/project/[slug]` migrates; layout hoist (C3) must account for the project route's data-derived nav theme.

**Out-of-scope, flagged only (not for this redesign):** S4 image opt on `LatestProjects`/`ProjectsGrid`; N5 (Footer LinkedIn URL); N1 (dead `category` coalesce).

## Phase 3 follow-ups (Nav/Footer + siteSettings)

- **Run `npm run typegen`** — the new `SITE_SETTINGS_QUERY` isn't in the generated types yet; `SiteSettings` is hand-written in `types/sanity.ts` for now (consistent with the transitional pattern).
- **Populate the `siteSettings` singleton** in Studio (`/studio` → Site Settings). Until then, Nav/Footer render hardcoded fallbacks matching the old content.
- **Client fixes via CMS:** placeholder phone `+961 1 234 567` and the LinkedIn URL (`company/addmindhospitality`, audit N5) — both still in the footer _fallback_; correct them by filling siteSettings.
- **"Join Us" nav link is provisional** pending the open `/join-us` decision (it's in `DEFAULT_NAV`).
- **Visual tune:** the inline `Logo` uses a cropped viewBox (`90 395 860 275`); verify in-browser it isn't clipped and looks right at header/footer sizes.
- **Out-of-scope note:** `/portfolio` + `/project/[slug]` now show the new global Nav/Footer (intended). The new fixed header is slightly taller — eyeball their top spacing.

## Phase 4 status (landing page)

- **Built (2026-07-09):** `homePage` singleton (hero video+image / intro / projects / studio / showcase), `HOME_PAGE_QUERY` + card projection + fallbacks, `.theme-redesign` base, `urlForSized` helper, `Reveal` wrapper, five server-component sections, `page.tsx` rebuilt. Old landing components (Hero, FeaturedProject, AboutSection, InstagramGrid, Recognition) deleted — no external importers; `Section`/`LatestProjects` kept for out-of-scope pages.
- **Carry-forwards resolved by construction:** S2 (no waterfall — one query, embedded derefs), S3 (server components; only `Reveal` is client), S4 (`urlForSized` + `sizes`, no `quality={100}`), N2 (no `url() || ""`), N6 (old animation hook not used by new pages).
- **User runtime steps:** populate Home Page in `/studio` (hero image required; optional video; pick projects; studio images; showcase project); visual pass vs `design-refs/landing-page/`.
- **Dropped from old landing (flag):** InstagramGrid + Recognition content has no home on the new landing by design — press could resurface on About (Phase 5) if wanted.

## Changelog

- **2026-07-05** — Tracker created. Criticals triaged: C1–C3 deferred to Phase 3 as rebuild constraints; C4 blocked pending typegen decision.
- **2026-07-05** — Decision: adopt TypeGen. Wired the pipeline: `typegen` block added to `sanity.cli.ts`, `src/sanity/extract.json` gitignored (S5 ✅). Installed deps (needed `--legacy-peer-deps` → logged as S6). Type generation blocked in-sandbox: Sanity v5 `schema extract` calls the project API and fails on a placeholder ID — needs an authenticated run of `npm run typegen`. No consuming code changed; out-of-scope pages untouched.
- **2026-07-05** — User generated `sanity.types.ts` locally. Caught that `overloadClientMethods: true` broke `next build` at the out-of-scope project page; set it to `false`, regenerated-equivalent output, verified `tsc --noEmit` clean. **C4 done.** All four criticals resolved (C4 fixed; C1–C3 documented as Phase-3 constraints).
- **2026-07-05** — Folded S6 into this pass: added `.npmrc` (`legacy-peer-deps=true`); verified a flagless `npm install` now succeeds. Left unstaged for user to commit. Awaiting green light before starting the remaining should-fix items (S1–S5).
- **2026-07-09** — Phase 4 iteration 3: feeds restructured from arbitrary scatter to the reference's **wide-card + offset-pair row grid** (reads intentional, shared rows); magazines removed from cards entirely → new **`studioSection.mentions`** schema field rendered as a text-only hairline press index (publication + line + ↗), defaults carry all old press + the image-less magazine features (NOUN/Love That Design/AD). tsc/eslint/typegen clean.
- **2026-07-09** — Phase 4 iteration 2 (user notes): headings Archivo→**Manrope**; intro statement smaller/tighter; sticky columns got a bottom image (new CMS field on both sections); StudioTeaser→**StudioFeed** — same treatment as ProjectsFeed but scrolling cards = press mentions + About Us + Join Us (`studioSection.cards` replaces `images`; old Recognition content as placeholder defaults); shared `FeedCard` extracted so both feeds stay identical; placeholders from existing public assets wherever CMS images are missing. tsc/eslint/typegen clean.
- **2026-07-09** — **Phase 4 built** (landing page, schema-first, k-studio structure adapted to our tokens per user refs). tsc/eslint/typegen all clean — typegen now runs locally (CLI reads .env.local). Zero out-of-scope files touched; deletions limited to wiped landing components. Details in "Phase 4 status" above.
- **2026-07-06** — Phase 3 design iteration 2: footer rebuilt against user refs (`design-refs/footer/`, primary: eleven.png) — inverted warm-dark ground (tokens flipped via color-mix, no new hex), two-zone layout with full-height vertical hairline, Menu/Studios/Connect index, hairline + meta row. Footer now also renders nav links (from siteSettings). Header: no border, more solid frosted bar + faint shadow, logo 34px→28px compress on scroll. Fonts switched Fraunces→Archivo earlier same day.
- **2026-07-06** — **Phase 3 built** (Nav + Footer + siteSettings singleton). Schema-first siteSettings (nav/contact/locations/socials) as a Studio singleton; Nav/Footer self-fetch it (so out-of-scope routes need no prop changes) with hardcoded fallbacks. New header = logo-left / all-caps links-right, sticky, transparent→frosted "mirror" on scroll, theme-aware color (keeps `theme` prop, C2). New editorial footer, warm tokens, no gradient (keeps `showGradient`, C1). Reusable inline `Logo` (currentColor). tsc + eslint clean; zero out-of-scope files touched. See Phase 3 follow-ups above.
- **2026-07-05** — Should-fix batch. **S1 done** (`queries.ts` LIST/DETAIL split; `tsc` clean). Flagged that **S2/S3/S4-in-scope target Phase-4-wiped files** → recommended deferring into the rebuild rather than fix-then-delete; S4's durable targets are out-of-scope portfolio and left untouched.
- **2026-07-05** — User chose to defer S2–S4 to Phase 4. Recorded as confirmed deferrals + added a "Phase 4 carry-forward" section so the rebuild implements them by construction. **Phase 1.5 audit remediation complete:** actioned C1–C4, S1, S5, S6; remainder deferred to Phase 2–4 with tracked rationale.
- **2026-07-05** — **Phase 2 done** (tokens + fonts). Added warm-neutral + brown token set + Fraunces/Inter to `globals.css`/`layout.tsx`; deprecated (kept) `--surface-gradient`/`--font-script`; corrected REDESIGN.md aesthetic rules (user kept brown, not monochrome). Old element defaults left on legacy tokens so out-of-scope pages are untouched. `tsc` clean; diff isolated to 3 files. **Runtime/visual verify (needs Sanity env) is the user's step.** Phase 3/4 carry-forward: add `.theme-redesign` base class; post-redesign cleanup deletes deprecated tokens + drops the 3 old font loads.
- **2026-07-23** — Reference changed: k-studio.gr → vincentvanduysen.com. Scope also expanded (project schema rebuild, portfolio→archive, landing becomes project-viewer). REDESIGN.md rewritten to reflect this; phases renumbered there. Everything above this line describes the superseded k-studio-era build.
- **2026-07-27** — **Studio narrative restructure (Phase 8.1).** New order: founder (h1 lead) → hairline separator → firm description (text only, was StudioIntro w/ images) → Sectors/Services (text only, was StudioDisciplines w/ images). **Team text dropped** (user: recruiting lives on Join Us), **all project/interior photos removed** from the about page (only the founder portrait remains), **Publications + Contact split out to their own pages** (roadmap Phase 8.2 Contact `/contact` reading siteSettings, 8.3 Publications reusing the parked `StudioPublications` + `studioPage.publications`). Schema trimmed: `studioPage.intro` lost image/secondaryImage/secondaryBody, `team` object removed, `disciplines` lost its images (clean-break — orphans any entered images, intended). `StudioTeam/` deleted; dead `.cta`/`.statement` CSS removed. typegen/tsc/eslint clean; smoke: `/studio` 200 with correct order, no project images, no team/publications/contact.
- **2026-07-27** — **Phase 8.2 (Contact page) built.** New `/contact` route + `page.module.css`: VVD `Contact.png` single left-aligned column — serif "Contact" `h1`, bold `brandLine`, locations (label + address lines), phone (`tel:`), email (`mailto:`), Instagram/LinkedIn/View Map rows (`OutwardIcon` ↗), quiet-muted links. Reads the existing `siteSettings` (no schema/query change), with footer-era fallbacks so it's never empty. **This finally gives the footer-era contact content a home** (closes the standing Phase-3 flag). Added **Contact** to `DEFAULT_SECONDARY_NAV` (Archive, Studio, Contact, Join Us). tsc/eslint clean; smoke: `/contact` 200 with all rows + nav link, other routes unaffected. **User step:** if `secondaryNav` is populated in `/admin`, add a Contact → /contact item (fallback covers otherwise).
- **2026-07-27** — **Phase 8.4 (splash screen) built.** New `SplashScreen` client component in the root layout: full-viewport `--color-bg` overlay (z-1000, above the nav), `Logo` in `--color-title` animating opacity 0→1 + **scale 0.5→1** (0.9s ease-out), brief hold, then the overlay fades out (0.6s) and **unmounts** (timeouts: leave @1300ms → pointer-events none, done @1900ms → `return null`). Rendered in the initial SSR HTML (no hydration flash); plays **once per hard load** (state persists across client nav; no replay on link clicks). `prefers-reduced-motion` skips the scale/fade-in. tsc/eslint clean; overlay+logo present in SSR HTML on `/` and other routes. Timings tunable; once-per-session gating is a noted trivial follow-up.
- **2026-07-27** — **Phase 9 started** (branch `phase-9-press-awards-polish`). Built sub-tasks 1–3, 5, 6: caption fixed-width **12rem** (left edge no longer shifts with title length); mobile header logo 24→**32px** (viewer mobile offset 64→72px to match); **universal LQIP** — all 4 `next/image` sites now always `placeholder="blur"` with `blurDataURL={lqip ?? FALLBACK_BLUR}` (shared warm-gray in `src/lib/blur.ts`), no more white flashes; **manual project ordering** via `@sanity/orderable-document-list` (`orderRankField` on project, `orderableDocumentListDeskItem` "Projects" in the desk, viewer + archive queries `order(orderRank)`); **Press submenu** — secondary rail nav is now a fixed nested structure (Archive, Studio, **Press[Publications, Awards]**, Contact, Join Us); Press is a client toggle (auto-expands on `/publications` `/awards`), rail + drawer; `siteSettings.secondaryNav` read **retired** (field unused — Phase 10 cleanup); minimal `/awards` placeholder route so the link resolves. tsc/eslint/typegen clean; smoke green. **Remaining:** sub-task 4 (mobile drag-follow) + sub-task 7 (Awards page — data-gated). **User steps:** drag-order Projects once in `/admin` (assigns ranks to existing docs); provide Awards data when ready.
- **2026-07-27** — Splash screen **removed** (user): deleted `SplashScreen` component + its render in the root layout. The middleware/`headers()`/`isStudio` logic stays — it still gates `SanityLive` off `/admin`.
- **2026-07-27** — **Bug fix: Studio flicker on image upload/edit.** `<SanityLive />` (frontend Live Content API) was rendered in the root layout, so it also ran inside the embedded Studio at `/admin`. Its live-content revalidation calls `router.refresh()` on every mutation → inside the Studio that flickered the UI, reset scroll, and closed/reopened the asset modal on upload. Fix: a `src/middleware.ts` sets an `x-pathname` request header; the root layout (now async, reads `headers()`) renders `SanityLive` + `SplashScreen` only when **not** under `/admin`. Removed `force-static` from the admin page (conflicts with the now-dynamic layout; the Studio is a client SPA so dynamic is fine). Tradeoff: the app now renders dynamically (headers() opts out of static generation) — negligible here since every page already fetches via the Live API. First attempt (a `"use client"` guard) failed — `defineLive`'s `SanityLive` is server-only. tsc/eslint clean; verified splash/live present on `/`, absent on `/admin`, all routes 200.
- **2026-07-27** — Nav UX (user): category filter is now a **removable tag** — the active category renders a trailing `✕` (same glyph as the mobile close) and a "Clear {category} filter" aria-label; clicking the item (or its ✕) clears via the existing `href="/"`. Fixes the discoverability gap where clearing the landing filter was invisible. Nav-only (NavigationClient + CSS), applies to rail + drawer; no schema/query change. tsc/eslint clean; verified ✕ present only when filtered.
- **2026-07-27** — **Phase 8.3 (Publications page) built.** New `publicationsPage` singleton (registered in `schemaTypes/index.ts` + `structure.ts` desk) — an ordered `publications[]` array (image/publication/date/description/url/linkLabel). New `/publications` route: `h1` "Publications" then **one publication per row, cover image left / content right** (publication+date line in title-brown 500, description paragraphs, "Full story here" ↗ link) — **no year grouping** (dropped from the VVD ref per user). `NativeImage` covers (native ratio, 12rem column; stacks on mobile), `IMAGE_META` query + generated types, footer-era press fallbacks (3 items with `/public/about/*` covers) so it's never empty. Added **Publications** to `DEFAULT_SECONDARY_NAV` (Archive, Studio, Publications, Contact, Join Us). tsc/eslint/typegen clean; smoke: `/publications` 200 (3 rows, links, no year headers), `/admin` lists the singleton, nav link present, other routes unaffected. **User step:** populate the Publications Page in `/admin`; add a Publications → /publications item if `secondaryNav` is CMS-populated.
- **2026-07-27** — Contact layout refinement (user): locations now a **two-column** grid, each with its own `T: <phone>` line, **all black** (no muted grey); below, an **Inquiries** section — black label, grey email, then Instagram/LinkedIn/View Map as black rows.
- **2026-07-27** — Studio Sectors/Services tweaks (user): added a CMS `heading` field ("What we do" default) rendered as the section's `h2` title above the intro line; lists now stay **two columns on mobile** (dropped the 1fr override, gap tightened to 1.25rem) with entries wrapping within their column.
- **2026-07-27** — Studio follow-up (user): **publications fully retired from `studioPage`** (was parked, but confusing in the CMS) — schema object + query projection + `StudioPublications` component + its CSS all removed; the Publications page (roadmap 8.3) will be built fresh with its own singleton. Also **reordered the studioPage schema groups/fields to match the frontend** (founder → About/Firm → Sectors & Services; founder now the default tab), so the CMS reads top-to-bottom like the page. typegen/tsc/eslint clean; `/studio` + `/admin` 200.
- **2026-07-26** — **Phase 8 (Studio + Join Us visual pass) built.** Studio rebuilt to VVD's faithful **single centered column** (user choice): all 5 sections kept (schema untouched) but grids/sticky/two-column dropped — images now **float free at native aspect ratio** (intrinsic dimensions, never cropped) via a new shared `NativeImage` component (Sanity metadata path + static-import fallback for `/public/about/*` placeholders, both native-ratio; drops the `quality={90}` S4 anti-pattern). One shared `studio.module.css` for the whole page; per-component CSS modules deleted. `Reveal` scroll animations **dropped** from these pages (user choice) — `Reveal` + `useScrollAnimation` are now fully orphaned (retire in Phase 9). Join Us: **kept the split video hero** (user choice), restyled to VVD-quiet type (serif heading, sans body, no all-caps/filled-button; email now a quiet underlined link). Queries extended with an `IMAGE_META` fragment (dimensions + lqip); **STUDIO/JOIN_US adopted generated types**, hand-written `StudioPage`/`JoinUsPage`/`PublicationItem` deleted from `types/sanity.ts`. Both pages dropped the dead `<Footer/>`. tsc/eslint/typegen clean; smoke: `/studio` 200 (single-column, no footer, no Reveal, images render), `/join-us` 200 (video hero), other routes unaffected. **User step:** populate the Studio Page + Join Us singletons in `/admin` (placeholders show until then); visual pass vs `About.png`.
- **2026-07-26** — Viewer transition rework (user note): crossfade → **directional carousel slide**. Incoming frame travels in from the edge the navigation points at, outgoing exits the opposite side (horizontal for image nav, vertical for project nav — mirrors the input axis); offsets are full-viewport via inline `--enter-x/y` / `--exit-x/y` custom props consumed in the keyframes; 0.5s ease-out curve; caption swaps instantly (plain React re-render, no animation); reduced-motion = instant swap. Gallery stack still preloads siblings. (Also fixed a self-inflicted CSS-comment bug: `*/` inside a comment describing the custom props terminated it early.)
- **2026-07-25** — **Nav/landing IA amendment (user-proposed, converges on VVD's actual model):** the rail's primary stack is now the **category list itself** (data-driven, empty categories hidden query-side; no "All"; user removes "Other" in CMS) and clicking a category filters the **landing rotation** via `/?category=slug` (URL state — landing reads the search param server-side and passes `activeCategory` to Navigation, so no `useSearchParams`/Suspense in the client). Clicking the active category clears the filter; logo always lands unfiltered. **Archive moved to the secondary stack** as a plain link (its nested sub-list + `/archive/[category]` route deleted — search covers category narrowing there). **`featured` dropped from the project schema** — the whole catalog is the rotation now; trade-off (what leads = newest) logged as backlog N7 (manual ordering). `siteSettings.nav` field retired (schema + query + type); secondary fallback = Archive/Studio/Join Us. typegen/tsc/eslint clean; smoke: filter link 200 + active state + clear-href, empty-category hiding verified live (only "beaches" renders), old category route 404s. **User steps:** delete unwanted category docs (e.g. "Other", duplicates); optionally set Secondary Navigation in `/admin`; delete stale `featured` values is unnecessary (field gone, data inert).
- **2026-07-25** — Phase 7 iteration 2 (user request, un-deferring the ref's search): new `ArchiveExplorer` client component per `archive with search.png` — hairline-underlined full-width field, right-aligned quiet "Search" placeholder, matches across title/location/status/category/year (query projection extended with `year` + dereffed category title). **Deliberately no debounce**: single server fetch, filtering is synchronous in-memory substring matching (no async work to coalesce; a debounce would only delay feedback); `useMemo`d lowercase haystacks + `useDeferredValue` give headroom at scale. "No matches." state + `aria-live` result count. Cards shrunk toward the ref (44rem grid, 4rem column gaps). typegen/tsc/eslint clean, smoke green.
- **2026-07-25** — **Phase 7 (Archive) built.** New `/archive` + `/archive/[category]` routes (route-based filtering — no query params, active states from pathname, unknown slug → 404): sparse 3-col grid (2-col mobile) of native-ratio thumbs with "Title, Location, Status" one-line captions in the title brown, per `Archive.png`; underline-on-hover; empty state. New `ARCHIVE_PROJECTS_QUERY` ($category param) + `CATEGORIES_QUERY`, generated types. **Rail sub-list**: All + categories indented under Archive (serif, 1rem deeper), shown only on archive routes, mirrored in the mobile drawer. `/portfolio` → `permanentRedirect("/archive")` (no next.config touch); nav fallback label now Archive. **Retired**: `LatestProjects`, `ProjectsGrid`, `Section`, `renderProjectTitle`, `LIST_FIELDS` + Phase-4 shims, `ALL_PROJECTS_QUERY`, old `Project`/`Category`/`PortableTextBlock`/`PortableTextSpan` types, and the orphaned `.animate-*`/`.visible` utility blocks in globals.css (`useScrollAnimation` stays — Reveal). S4/N1/N2/N3's portfolio-side items are moot (components gone). typegen/tsc/eslint clean; smoke: archive 200 w/ grid+sub-list, category route 200, bogus 404, portfolio 308→archive, landing/studio/detail unaffected. **Flags for user:** Studio currently has 6 category docs (`beaches` AND `beach-clubs`, `private-residential`, `lounges`…) vs the confirmed 5 — tidy duplicates/titles in `/admin`; update Site Settings nav item to Archive/`/archive` if populated.
- **2026-07-25** — **Regression fix (mobile landing):** the drawer de-bounce change made `.drawerInner`'s padding constant — but padding is a hard floor on box height that the collapsed 0fr track can't compress, so the "closed" drawer sat ~2.75rem open (first nav link permanently visible) and the extra header height pushed the viewer's `calc(100dvh - 64px)` layout past the viewport, cutting the bottom caption bar in half. Fix: padding gated on `.drawerOpen` again but **transitioned** on the same 0.45s curve as the grid rows — collapse is total, motion stays soft.
- **2026-07-25** — Phase 5/6 iteration 2 (user notes): detail mobile — images tightened to 24px side padding (text section keeps 48px), more air above the title (6rem), tighter title margin (0.75rem); landing mobile bar — CTA top margin removed; mobile drawer de-bounced — the inner padding used to pop in/out while the height animated (read as a bounce), now constant padding clipped by the drawer (`overflow: hidden`) + softer 0.45s cubic-bezier curve.
- **2026-07-25** — Phase 5/6 iteration (user notes): landing caption on **mobile only** becomes a full-width bottom bar (title/location far left, year/CTA far right, space-between; chevron hidden) — desktop keeps the stacked bottom-right block, markup shared via two caption groups + CSS-only switch. Detail page: mobile padding ~48px (3rem) around header AND images (no more full-bleed); desktop images now **free-sized at native ratio with dual guards** (`max-width: min(75vw, 1100px)` + `max-height: 82vh`) — the height guard makes portraits come out narrower than landscapes instead of everything inflating to one column width, and the 34rem hero column reads narrower than the images (reference hierarchy). tsc/eslint clean, both routes 200.
- **2026-07-25** — **Phase 6 (project detail page) built.** `/project/[slug]` rebuilt as VVD's detail: info header (serif title in `--color-title`, location+year as one left-aligned sans block per the Phase-5 caption rule, PortableText description with link mark, em-dash rule + credits) then the ordered `images[]` as a normal vertical scroll — native ratio via intrinsic width/height, 62vw/920px column, **mobile full-viewport-width**, LQIP, lazy. **No nav/footer — fixed close ✕** (`CloseButton` client component: same-origin referrer → `router.back()`, else `/`). New `PROJECT_DETAIL_QUERY` reuses `VIEWER_FIELDS` + description/credits, generated types. **C1+C2 closed:** `Navigation.theme` and `Footer.showGradient` props removed (rail scroll listener deleted with the light-flip; `/portfolio` call site fixed, 1-line flagged). **C3 closed by design** (see table). **PageBuilder React tree deleted** (7 components) + block interfaces/`PageBuilderBlock` union/`Project.content` pruned from hand-written types (`Project` card shape survives for Phase-7-doomed consumers; `renderProjectTitle`/`PortableTextBlock` kept). typegen/tsc/eslint clean; smoke: detail 200 (7 figures, close ✕, zero nav markup), bogus slug 404, `/`+`/portfolio`+`/studio` 200. **User step:** visual pass vs the two detail refs; check close ✕ and mobile full-width images.
- **2026-07-25** — Phase 5 iteration 2 (user notes): rail padding 2.5rem→**2.25rem** (36px), nav stack indent 1.25rem→**0.5rem** (mobile drawer matched); viewer caption regularized — year loses muted color + indent (same type as location, all left-aligned), CTA drops underline + text arrow for the provided `public/icons/chevron_right.svg` (applied as a `mask` over currentColor since the file hardcodes its fill), CTA color a touch lighter than body via color-mix, tighter 0.4rem separation. tsc/eslint clean.
- **2026-07-25** — **Phase 5 (landing project-viewer) built.** New `ProjectViewer` client component (the only client code on the landing): one featured project fills the viewport, one image at a time. Zone layout per `Cursor interaction guidelines.png` — top band 0–39% = prev project (∧ cursor), middle 39–61% split at center ‹/› = image nav, bottom = next project (∨); inline-SVG data-URI chevron cursors. **The one sanctioned scroll-jack:** wheel intercepted (`passive:false`), vertical→projects, horizontal→images, 50-delta threshold + 700ms inertia cooldown. Keyboard arrows, touch (swipe axes mirror desktop; tap opens detail; zones hidden on `hover:none`), wrap-around both axes, 400ms crossfade (keyframe fade-out for the outgoing project frame; reduced-motion safe), LQIP placeholders, height-locked images (72vh/60dvh mobile) at native ratio via `--ar` custom prop, neighbours preloaded. Caption bottom-right: serif title / location / muted year / **underlined "Go to project →" CTA**. Data: new `VIEWER_PROJECTS_QUERY` (+ non-featured fallback so the landing is never blank) using **generated typegen types** (first adopter). **k-studio landing deleted end-to-end:** 6 feed components, `homePage` schema+desk+query+`HomePage`/`ProjectCard`/`StudioCard` types (`Reveal`, `urlForSized` kept — studio/join-us consumers). Carry-forwards held: S2 (no waterfall), S3 (server-first), S4 (`sizes`+width caps), N2 (no `url() || ""`). **Found + fixed en route:** `/portfolio` 500 on docs without any image (old `urlFor(null)` N2 pattern) — hardened `ALL_PROJECTS_QUERY` with an image-presence filter instead of touching the out-of-scope component. typegen/tsc/eslint clean; smoke: all 4 routes 200, viewer renders "Be Beach" with 4 zones + CTA. **User step:** manual interaction pass (wheel/cursors/swipe/tap/crossfade) in a real browser.
- **2026-07-25** — **Phase 4 (project schema rebuild) built.** `project` rewritten to the minimal VVD model: title/slug/location(free string)/year(string, allows ranges)/status(radio, display-string values rendered verbatim in Archive captions)/category(single ref)/description(blocks: strong/em + link)/credits/**ordered `images[]` with per-image alt** (drives landing carousel AND detail scroll)/featured. `category` simplified to `{title, slug}` — color field + swatch machinery dropped. **Taxonomy confirmed (user): Residential / Restaurants / Beach Clubs / Lounges & Bars / Other** (hospitality-leaning, replaces REDESIGN's generic list). **Migration confirmed (user): clean break** — old docs to be deleted + re-entered manually in `/admin`. PageBuilder dropped **schema-side only**: `pageBuilder.ts` + 6 `blocks/*` schemas deleted, registrations removed; React components + hand-written types survive until Phase 6 (`/project/[slug]` still compiles; renders stale docs via schema-less GROQ until deletion, then empty state). Query shims in `queries.ts` keep old landing/portfolio working with new-schema docs (`coverImage→coalesce(coverImage, images[0])`, single `category`→old `categories[]` shape). `@sanity/color-input` uninstalled (see S6 update — `.npmrc` stays). typegen/tsc/eslint clean; smoke: `/`, `/portfolio`, `/admin`, `/project/bar-du-port` all 200. **User steps:** delete old project docs, create the 5 categories, re-enter projects (feeds empty until then — expected).
- **2026-07-25** — Phase 3 iteration 2 (user notes + new ref `mobile-menu-expanded.png`): mobile fullscreen overlay replaced with an **in-flow drawer** — header + drawer now sit in normal document flow (no longer `position: fixed`), so opening pushes page content down; 0fr→1fr grid-rows animation, hairline `border-bottom` only when open, hamburger toggles to ✕, same typography/left alignment as the desktop rail, active item underlined per the ref. Mobile bar consequently lost its scrolled/light overlay states (rail keeps them). Also `.rail-offset` gained a right-side balancing band: `padding-inline-end: clamp(2rem, 6vw, 6rem)`. tsc/eslint clean, smoke-verified.
- **2026-07-25** — **Phase 3 (VVD nav + footer) built.** Top bar → fixed full-height **left rail**: SVG logo (title-brown, user choice over a text wordmark) + serif primary stack beneath it + sans secondary stack pinned bottom-left; rail is pure whitespace (no bg/border, `pointer-events` pass-through). Nav split into work/info stacks (user choice) via new **`siteSettings.secondaryNav`** field (schema + query + hand-written type + typegen; fallbacks: Portfolio / Studio+Join Us). **Footer removed entirely** (user choice, truest to VVD): renders `null`, `showGradient` API kept (C1), `Footer.module.css` deleted; contact content (locations/phone/email/socials/mapUrl) now has **no rendered home until Phase 8** — fields untouched in CMS, Studio group retitled "Contact (not currently displayed)". `theme="light"` still functional on the rail (C2): light over dark hero, flips dark on scroll. New `--rail-width` token + `.rail-offset` utility; applied to `/studio`, `/join-us`, and (flagged 1-line out-of-scope touch) `/portfolio`. `/project/[slug]` deliberately NOT offset — its content path is a full-bleed dark PageBuilder hero with `theme="light"`, same overlay rationale as the landing (deviation from plan, reasoned). Mobile: slim top bar + quiet left-aligned fullscreen menu (both stacks). tsc/eslint/typegen clean; smoke-verified: 4 routes 200, zero `<footer>` in HTML, Primary/Secondary landmarks present. **User step:** populate Secondary Navigation in `/admin` Site Settings (fallbacks cover until then).
- **2026-07-24** — **Phase 2 (VVD token pass) built.** Palette taken from evidence, not eyeballing: pixel-sampled `/design-refs/vvd/` screenshots and cross-checked against vincentvanduysen.com's live CSS (values matched). Revalued the redesign tokens in place: `--color-bg` → `#fffffa`, `--color-fg` → `#000`, `--color-fg-muted` → `#a99b9b`, new `--color-title: #391812` (VVD's brown-black), `--color-line` now derived via color-mix, `--tracking-heading` → 0. `--color-accent` aliased to the title brown + marked `@deprecated` (with `--tracking-label`); removal in Phase 9. Added measured type-scale tokens (`--text-*`, `--leading-body`, `--tracking-body`). Fonts: VVD uses Custodia Pro + News Gothic Std (both commercial) → user chose **EB Garamond** (serif display) + **Libre Franklin** (body sans); Manrope/Inter loads removed (grep-verified unconsumed), legacy Cormorant/Darker Grotesque/Great Vibes kept for out-of-scope pages. Existing k-studio-era components re-skin transitionally by design; `/portfolio` + `/project/[slug]` consume only legacy tokens and are untouched. `tsc` clean. Visual pass happens per-phase as components rebuild.
